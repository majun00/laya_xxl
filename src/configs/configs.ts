/** 整个游戏的宽度 */
const CONTAINER_WIDTH = 600;

/** 整个游戏的高度 */
const CONTAINER_HEIGHT = 700;

/** 整个画布的宽度 */
const BG_WIDTH = CONTAINER_WIDTH;

/** 画布的高度 */
const BG_HEIGHT = 600;

/** 生成的行数 */
const COUNT_ROW = 8;

/** 生成的列数 */
const COUNT_COLUMN = 8;

/** 每个方块的宽度 */
const WIDTH_BLOCK = BG_WIDTH / COUNT_COLUMN;

/** 每个方块的高度 */
const HEIGHT_BLOCK = BG_HEIGHT / COUNT_ROW;

/** 字体大小 */
const FONT_SIZE = 40;